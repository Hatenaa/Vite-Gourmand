{# Desktop : liens directs #}
<nav class="d-none d-md-flex gap-4 ms-auto">
	{% for link in nav_links('admin') %}
		<a href="{{ path(link.route) }}" class="text-white text-decoration-none">{{ link.label }}</a>
	{% endfor %}
</nav>

<button class="d-md-none btn btn-link text-white p-0 ms-auto" type="button" data-bs-toggle="offcanvas" data-bs-target="#dashboardNav" aria-controls="dashboardNav">
	<i class="bi bi-list fs-2"></i>
</button>