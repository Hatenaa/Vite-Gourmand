<article>

	{% if menu.images|length > 0 %}
		{% set mainImage = menu.images|first %}

		<img src="{{ asset(mainImage.path) }}" alt="{{ mainImage.alt ?? menu.title }}" width="300">
	{% endif %}
    
	<h3>{{ menu.title }}</h3>

	<p>{{ menu.description }}</p>

	<p>{{ menu.basePrice }}</p>

	<span>{{ menu.minPeople }}</span>

</article>
